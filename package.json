{
  "name": "fabres",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "eslint": "latest",
    "prettier": "latest",
    "turbo": "latest",
    "eslint-config-custom": "workspace:*",
    "@krapjost/prettier-config": "^1.0.0"
  },
  "prettier": "@krapjost/prettier-config",
  "engines": {
    "node": ">=14.0.0",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@8.0.0"
}
