import CollaborationCursor from '@tiptap/extension-collaboration-cursor'

// provider: WebrtcProvider | WebsocketProvider
// WebrtcProvider from 'y-webrtc' is a Yjs provider that uses WebRTC to sync
// WebsocketProvider from 'y-websocket' is a Yjs provider that uses WebSockets to sync
export default CollaborationCursor
