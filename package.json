{
  "name": "knot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build": "turbo run build",
    "web": "pnpm --filter web --",
    "desktop": "turbo run --filter=desktop --"
  },
  "devDependencies": {
    "dprint": "^0.35.4",
    "husky": "^8.0.3",
    "pm2": "^5.3.0",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0",
    "pnpm": ">=8",
    "npm": "pnpm",
    "yarn": "pnpm"
  }
}
