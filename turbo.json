{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["ui:watch"],
      "cache": false,
      "persistent": true
    },
    "ui:watch": {},
    "lint": {}
  }
}
