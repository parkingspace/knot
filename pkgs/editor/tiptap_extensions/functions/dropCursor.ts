import Dropcursor from '@tiptap/extension-dropcursor'

export default Dropcursor.configure({
  // Color of the dropcursor.
  // Default: 'currentColor'
  color: 'currentColor',

  // Width of the dropcursor.
  // Default: 1
  width: 1,
})
