{
  "name": "knot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build": "turbo run build",
    "web": "pnpm --filter web",
    "desktop": "pnpm --filter desktop"
  },
  "devDependencies": {
    "husky": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=8",
    "npm": "pnpm",
    "yarn": "pnpm"
  },
  "packageManager": "pnpm@8.0.0",
  "dependencies": {
    "flexsearch": "^0.7.31"
  }
}
